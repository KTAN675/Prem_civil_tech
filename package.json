{
  "name": "prem-civil-tech-root",
  "version": "1.0.0",
  "description": "Root workspace manager for Prem Civil Tech Solutions",
  "scripts": {
    "setup": "npm run setup --prefix backend && npm install --prefix frontend && npm install --prefix admin_frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin_frontend",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\""
  }
}
